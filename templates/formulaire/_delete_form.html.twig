<form method="post" action="{{ path('formulaire_delete', {'idForm': formulaire.idForm}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ formulaire.idForm) }}">
    <button class="btn">Delete</button>
</form>
