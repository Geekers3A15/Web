{% extends 'base.html.twig' %}

{% block title %}Formulaire{% endblock %}

{% block body %}
    <h1>Formulaire</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>IdForm</th>
                <td>{{ formulaire.idForm }}</td>
            </tr>
            <tr>
                <th>IdUser</th>
                <td>{{ formulaire.idUser }}</td>
            </tr>
            <tr>
                <th>ServiceDemande</th>
                <td>{{ formulaire.serviceDemande }}</td>
            </tr>
            <tr>
                <th>NumTel</th>
                <td>{{ formulaire.numTel }}</td>
            </tr>
            <tr>
                <th>DateLimite</th>
                <td>{{ formulaire.dateLimite }}</td>
            </tr>
            <tr>
                <th>Status</th>
                <td>{{ formulaire.status }}</td>
            </tr>
            <tr>
                <th>DateCreation</th>
                <td>{{ formulaire.dateCreation ? formulaire.dateCreation|date('Y-m-d') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('formulaire_index') }}">back to list</a>

    <a href="{{ path('formulaire_edit', {'idForm': formulaire.idForm}) }}">edit</a>

    {{ include('formulaire/_delete_form.html.twig') }}
{% endblock %}
